<%= form_with(model: raman, local: true) do |form| %>
  <% if raman.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(raman.errors.count, "error") %> prohibited this raman from being saved:</h2>

      <ul>
      <% raman.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
